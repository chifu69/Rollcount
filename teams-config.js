/* Roll Count GitHub + Teams configuration.
   Leave blank for first-run setup inside the app.
   IT may optionally prefill these values before publishing to GitHub Pages. */
window.ROLLCOUNT_TEAMS_DEFAULTS = Object.freeze({
  clientId: '',
  sharedDataUrl: ''
});
